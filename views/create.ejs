<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Stylish Form</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background: linear-gradient(135deg, #1c92d2, #f2fcfe);
        }

        .main {
            background: rgba(0, 0, 0, 0.7);
            padding: 30px 20px;
            width: 90%;
            max-width: 400px;
            border-radius: 12px;
            box-shadow: 0px 8px 15px rgba(0, 0, 0, 0.2);
            text-align: center;
            color: #fff;
        }

        .heading {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #00e1ff;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        textarea,
        input[type="file"] {
            margin: 10px 0;
            padding: 12px;
            font-size: 1rem;
            border: 1px solid #00e1ff;
            border-radius: 8px;
            background: transparent;
            color: #fff;
            outline: none;
            transition: all 0.3s ease;
        }

        textarea::placeholder {
            color: #a9a9a9;
        }

        textarea:focus,
        input[type="file"]:focus {
            border-color: #ff0080;
            box-shadow: 0 0 8px rgba(255, 0, 128, 0.8);
        }

        .registor {
            background: linear-gradient(135deg, #ff0080, #ff8c00);
            border: none;
            color: white;
            cursor: pointer;
            font-size: 1rem;
            padding: 12px;
            border-radius: 8px;
            margin-top: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .registor:hover {
            transform: translateY(-3px);
            box-shadow: 0px 5px 10px rgba(255, 140, 0, 0.5);
        }

        .sig {
            margin-top: 15px;
            font-size: 0.9rem;
            color: #ddd;
        }

        .sig a {
            color: #00e1ff;
            text-decoration: none;
            font-weight: bold;
        }

        .sig a:hover {
            color: #ff0080;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            .heading {
                font-size: 1.7rem;
            }

            textarea,
            input[type="file"] {
                font-size: 0.9rem;
                padding: 10px;
            }

            .registor {
                font-size: 0.9rem;
                padding: 10px;
            }

            .sig {
                font-size: 0.8rem;
            }
        }
    </style>
</head>
<body>
    <div class="main">
        <h1 class="heading">Submit Details</h1>
        <form action="/createPost" method="post" id="form" enctype="multipart/form-data">
            <textarea class="details" placeholder="Enter your details" name="dec" rows="4" required></textarea>
            <input type="file" class="file-upload" name="file" required>
            <input type="submit" value="Submit" name="btn" class="registor">
        </form>
        
    </div>

   
</body>
</html>
